# Portfolio

This project is a portfolio app built with NextJs, Typescript and Tailwind CSS.

## 🛠 How to Run Locally (Development Mode)

### ✅ Requirements

- [Docker](https://docs.docker.com/engine/install/) installed on your system

### 🔧 Setup Instructions

From the project root directory (where the `docker-compose.yml` file is located):

1. Run `docker compose up -d`.
2. Application will be available at `http://localhost:3000`.

